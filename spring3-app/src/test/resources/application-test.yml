spring:
  application:
    name: spring3-app-test

server:
  port: 0  # Use a random port for testing

# Disable actuator endpoints for tests
management:
  endpoints:
    web:
      exposure:
        include: health